import template from './order-list.html';
import controller from './order-list.controller';

const orderListComponent = {
  bindings: {},
  template,
  controller
};

export default orderListComponent;
